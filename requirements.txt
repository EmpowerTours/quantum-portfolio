# Top-level runtime dependencies. Pinned to the versions verified against
# the cached hardware run in outputs/hardware_run.json (Python 3.12).
qiskit==2.4.1
qiskit-aer==0.17.2
qiskit-ibm-runtime==0.47.0
qiskit-optimization==0.7.0
qiskit-finance==0.4.1
qiskit-algorithms==0.4.0
numpy>=1.26,<3
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5
streamlit==1.57.0
yfinance==1.3.0
requests>=2.31
python-dotenv>=1.0
# PQ signing: ML-DSA-65 (FIPS 204) and SLH-DSA-SHAKE-128s (FIPS 205) via
# quantcrypt's precompiled PQClean bindings — no C toolchain at install.
quantcrypt>=1.0.1
# Classical Ed25519 leg of the hedged signature (already a transitive dep
# via several others, listed explicitly to make the security stack obvious).
cryptography>=44.0
# Ethereum keccak256 (NOT the same as NIST SHA-3-256; hashlib.sha3_256
# would give wrong results). Used by src/monad_tx.py:_keccak256 for
# pool-label hashing and ABI function selectors. pycryptodome provides
# Crypto.Hash.keccak; eth_hash with pycryptodome backend is the
# canonical Ethereum-tooling choice.
pycryptodome>=3.20
